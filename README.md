## Zoo_app

### How to use:

- Run `git clone git@github.com:nax3t/Zoo_app.git` in your terminal to clone this app
- `cd` into `Zoo_app`
- Run `bundle`
- Run `rake db:create`
- Run `rake db:migrate`
- Run `rake db:seed` (optional)
- Run `rails s`